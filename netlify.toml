[build]
  command = "pip install -r requirements.txt && python build.py"
  publish = "output"  # or "site", "dist", "build"

[build.environment]
  PYTHON_VERSION = "3.9"

# Redirects for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
